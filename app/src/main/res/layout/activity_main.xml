<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="vm"
            type="me.laotang.carry.mvvm.demo.ui.store.MainViewModel" />

        <variable
            name="listener"
            type="me.laotang.carry.mvvm.demo.ui.view.MainActivity.ListenerHandler" />

    </data>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.view.MainActivity">

        <EditText
            android:id="@+id/et_input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:textStr="@{vm.lastIdQueried}"
            app:textChanges="@{vm.lastIdQueriedChanges}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toLeftOf="@id/bt_test"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/bt_test"
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:text="搜索"
            app:click="@{listener.userInfoRequestConsumer}"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_content"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:textStr="@{vm.usersLiveData}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/et_input" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>